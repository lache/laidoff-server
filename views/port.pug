html
    include ./includes/head.pug
    body.mar00.fwht.bsea.f1
        include ./includes/stat.pug

        a.dpinblk.red.pad5(href='/idle?u=' + user.guid) 취소

        div.mar0a.blk8.w70
            div.pad2.tc.f1p5 항구 선택

        table.mar0a.blk.w70
            each row in rows
                tr
                    each col in row
                        td.wspre
                            | #{col.name}
                            | 경도: #{col.x}
                            | 위도: #{col.y}
                        td
                            a.pad5.tc.dpblc.wblue(href='/movetoport?u=' + user.guid + '&region=' + col.region_id) 선택
