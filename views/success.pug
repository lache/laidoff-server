html
  include ./includes/head.pug
  body  
    center
      h1
        a.dpinblk.red.pad5(href='/idle?u=' + user.guid) Mission Completed!
    hr
    center 
      h3 #{mission.dept_name} - #{mission.arvl_name}
